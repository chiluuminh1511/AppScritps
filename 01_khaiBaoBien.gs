function Gsheet_datbien() { // tên hàm đặt tên theo con lạc đà 
  // khai báo biến trong hàm  
  // key work khai báo biến: var
  var str="Gsheet lmc"; //khai báo một biến kiểu dữ liệu string (text)
  var number=2; //khái báo biến kiểu dữ liệu số
  var dungsai=true; //khai báo biến kiểu dữ liệu bool
  var mang=["google sheet",11212025,true] //khai báo biến mảng

  Logger.log(number);
  Logger.log(mang[0]); //mảng bắt đầu từ 0
  Logger.log(mang[1]);
  Logger.log(mang[2]);

}
